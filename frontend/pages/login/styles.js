import styled from "styled-components";
import { 
    RegistrationContainer as BaseRegistrationContainer,
    FormContainer as BaseFormContainer,
    FieldContainer as BaseFieldContainer,
    Label as BaseLabel,
    Input as BaseInput,
    Button as BaseButton, 
    FormName as BaseFormName,
} from "../register/styles";

export const LoginContainer = styled(BaseRegistrationContainer)``;

export const FormContainer = styled(BaseFormContainer)``;

export const FieldContainer = styled(BaseFieldContainer)``;

export const Label = styled(BaseLabel)``;

export const Input = styled(BaseInput)``;

export const Button = styled(BaseButton)``;

export const FormName = styled(BaseFormName)``;